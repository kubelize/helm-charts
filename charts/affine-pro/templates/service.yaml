apiVersion: v1
kind: Service
metadata:
  name: {{ include "affine-pro.fullname" . }}
  labels:
    {{- include "affine-pro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "affine-pro.selectorLabels" . | nindent 4 }}
