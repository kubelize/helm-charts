apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "affine-notion.fullname" . }}-test-connection"
  labels:
    {{- include "affine-notion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "affine-notion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
