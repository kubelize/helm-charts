apiVersion: v1
kind: Service
metadata:
  name: {{ include "affine-notion.fullname" . }}
  labels:
    {{- include "affine-notion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "affine-notion.selectorLabels" . | nindent 4 }}
