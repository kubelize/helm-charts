apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbot-marcus.fullname" . }}
  labels:
    {{- include "dbot-marcus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbot-marcus.selectorLabels" . | nindent 4 }}
