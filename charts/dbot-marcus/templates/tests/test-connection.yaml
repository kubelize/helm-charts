apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbot-marcus.fullname" . }}-test-connection"
  labels:
    {{- include "dbot-marcus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbot-marcus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
